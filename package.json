{
  "name": "url-shortener",
  "version": "1.0.0",
  "description": "HTTP API for shortening URLs, redirecting visitors and tracking hits",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "start": "node dist/server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "keywords": [
    "url-shortener",
    "express",
    "typescript"
  ],
  "license": "MIT",
  "private": true,
  "dependencies": {
    "express": "^4.21.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.16.10",
    "@types/supertest": "^6.0.2",
    "supertest": "^7.0.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}