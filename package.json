{
  "name": "url-shortener",
  "version": "1.0.0",
  "description": "HTTP API for URL shortening, redirection and redirect statistics with in-memory storage",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "start": "node dist/server.js"
  },
  "engines": {
    "node": ">=20 <21"
  },
  "license": "MIT",
  "dependencies": {
    "express": "~4.21.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.17.0",
    "@types/supertest": "^6.0.2",
    "supertest": "~7.0.0",
    "typescript": "~5.7.2",
    "vitest": "^2.1.8"
  }
}