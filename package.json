{
  "name": "url-shortener",
  "version": "1.0.0",
  "description": "URL shortening HTTP API — encurtamento e redirecionamento de URLs",
  "engines": {
    "node": ">=20 <21"
  },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "start": "node dist/server.js"
  },
  "dependencies": {
    "express": "^4.21.2"
  },
  "devDependencies": {
    "@types/express": "^5.0.1",
    "@types/node": "^20.17.23",
    "@types/supertest": "^6.0.2",
    "supertest": "^7.1.0",
    "typescript": "^5.8.3",
    "vitest": "^2.1.9"
  }
}