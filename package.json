{
  "name": "url-shortener",
  "version": "1.0.0",
  "description": "API HTTP de encurtamento e redirecionamento de URLs",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "dev": "tsx src/server.ts",
    "start": "node dist/server.js"
  },
  "engines": {
    "node": ">=20 <21"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@types/node": "^20.17.0",
    "@types/supertest": "^6.0.2",
    "express": "^5.1.0",
    "supertest": "^7.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}